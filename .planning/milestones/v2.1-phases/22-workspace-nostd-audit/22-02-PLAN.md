---
phase: 22-workspace-nostd-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/nostd-audit.md
autonomous: false
requirements:
  - NOSTD-01
must_haves:
  truths:
    - "docs/nostd-audit.md exists and is committed to the repository"
    - "Every esp-idf-svc, esp-idf-hal, and esp-idf-sys usage in the firmware is enumerated by category (WiFi STA, WiFi SoftAP, NVS, OTA, UART, TLS, HTTP server, MQTT client, DNS, system calls, log hook, SNTP)"
    - "Each enumerated usage has a no_std/embassy equivalent or a documented gap with specific blocker"
    - "SoftAP password is marked RESOLVED (esp-radio supports WPA2Personal)"
    - "DNS hijack is marked SOLVABLE (embassy-net UDP socket, ~50 lines)"
    - "Log hook (vprintf) is marked SOLVABLE (C FFI, always available in esp-hal context)"
    - "Gap priority table ranks NVS, OTA, SoftAP+DNS, log hook, TLS, HTTP server, MQTT client, SNTP in that order with Phase assignments"
  artifacts:
    - path: "docs/nostd-audit.md"
      provides: "Complete ESP-IDF dependency audit mapped to embassy/nostd equivalents or gaps"
      contains: "Gap Priority Ranking"
  key_links:
    - from: "docs/nostd-audit.md"
      to: "Phase 23-25 gap crate work"
      via: "Gap Priority Ranking table with Phase assignments"
      pattern: "Phase 2[3-5]"
---

<objective>
Write and commit the complete nostd audit document that maps every ESP-IDF dependency in the firmware to an embassy/esp-hal equivalent or documents the specific blocker preventing a no_std port today.

Purpose: Satisfy NOSTD-01. Provides the authoritative reference for Phase 23-25 gap crate implementation decisions. Captures SoftAP/DNS/log-hook "RESOLVED/SOLVABLE" status to prevent re-litigating these in later phases.

Output: docs/nostd-audit.md committed to the repository.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-workspace-nostd-audit/22-RESEARCH.md

The research document contains the complete audit tables (all 12 categories with module, ESP-IDF API, no_std equivalent, and status columns). The audit document to be written IS this content, structured for readability and committed as the authoritative project reference.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/nostd-audit.md from research audit tables</name>
  <files>docs/nostd-audit.md</files>
  <action>
    Create the docs/ directory and write docs/nostd-audit.md as a standalone, self-contained reference document. The content comes from the 22-RESEARCH.md audit tables — do not abbreviate or summarise; copy the full category tables verbatim.

    The document structure must be:

    ```markdown
    # ESP-IDF Nostd Audit

    **Date:** 2026-03-12
    **Scope:** All `esp-idf-svc`, `esp-idf-hal`, `esp-idf-sys`, and `embedded-svc` usages in the v2.0 firmware
    **Purpose:** Map each usage to an embassy/esp-hal equivalent or document the specific blocker preventing a no_std port

    ## Status Legend

    | Status | Meaning |
    |--------|---------|
    | SOLVABLE | No_std equivalent exists; implementation straightforward |
    | RESOLVED | Was previously a blocker; now solved in current ecosystem |
    | GAP | No complete no_std equivalent; specific blocker documented |
    | REPLACED | ESP-IDF concept does not map to embassy; firmware approach changes |
    | UNKNOWN | Needs verification before Phase 23-25 implementation |

    ## Categories

    ### 1. WiFi — STA Mode
    [table from research]

    ### 2. WiFi — SoftAP Mode
    [table from research, note RESOLVED for SoftAP password]

    ### 3. NVS (Non-Volatile Storage)
    [table from research]

    ### 4. OTA
    [table from research]

    ### 5. UART
    [table from research]

    ### 6. TLS
    [table from research, include TLS blocker detail paragraph]

    ### 7. HTTP Server (SoftAP Portal)
    [table from research]

    ### 8. MQTT Client
    [table from research]

    ### 9. DNS Hijack (Captive Portal)
    [table from research, note SOLVABLE with embassy-net UDP socket]

    ### 10. System Calls (esp-idf-sys direct)
    [table from research, include REPLACED note for uxTaskGetStackHighWaterMark]

    ### 11. Log Hook
    [table from research, include SOLVABLE note for vprintf C FFI]

    ### 12. SNTP
    [table from research]

    ## Gap Priority Ranking

    Per Phase 22 success criteria, NVS/OTA/SoftAP/DNS/log hook are explicitly ranked for Phase 23-25 implementation order.

    [ranked table from research with Rank, Capability, Phase, Rationale columns]

    ## Key Findings

    **RESOLVED items (previously thought to be gaps):**
    - SoftAP WPA2 password: supported in esp-radio 0.16.x via `AccessPointConfig::with_password().with_auth_method(AuthMethod::Wpa2Personal)` — not a gap
    - DNS hijack: implementable with embassy-net UDP sockets (~50 lines); not an ESP-IDF-specific feature
    - Log hook (vprintf): `esp_log_set_vprintf()` is a C ROM function available in any esp-hal build; the C shim works unchanged

    **Hard GAPs for full embassy port:**
    - TLS: embedded-tls supports TLS 1.3 client only; AUSCORS NTRIP (port 443) requires TLS 1.2; no CA bundle in no_std
    - HTTP server: no production-ready no_std HTTP server with multi-field form POST parsing
    - MQTT client: minimq/rust-mqtt exist but maturity gap vs ESP-IDF MQTT client
    - OTA: esp-hal-ota 0.4.6 ESP32-C6 support unconfirmed ("cannot test if it works properly on esp32c6")
    - NVS: sequential-storage on ESP32-C6 flash unverified; build/hardware test needed in Phase 23

    ## State of the Art Notes

    [include the state-of-the-art table from research: esp-wifi->esp-radio, esp-hal-embassy->esp-rtos, SoftAP password RESOLVED, esp-storage archived into esp-hal]
    ```

    Populate every section with the full content from 22-RESEARCH.md. Do not truncate any table rows. The document should be self-contained — a reader who has not seen the research file must be able to use this audit to make implementation decisions in Phases 23-25.

    After writing the file, stage it: `git add docs/nostd-audit.md`
  </action>
  <verify>
    <automated>ls /home/ben/github.com/bharrisau/esp32-gnssmqtt/docs/nostd-audit.md && grep -c "GAP\|SOLVABLE\|RESOLVED" /home/ben/github.com/bharrisau/esp32-gnssmqtt/docs/nostd-audit.md && grep "Gap Priority Ranking" /home/ben/github.com/bharrisau/esp32-gnssmqtt/docs/nostd-audit.md && echo "audit document OK"</automated>
  </verify>
  <done>
    - docs/nostd-audit.md exists
    - Document contains all 12 categories (WiFi STA, WiFi SoftAP, NVS, OTA, UART, TLS, HTTP server, MQTT client, DNS, system calls, log hook, SNTP)
    - Each category table has at least one GAP, SOLVABLE, RESOLVED, REPLACED, or UNKNOWN entry
    - Gap Priority Ranking table is present with 8 ranked entries
    - Key Findings section documents the three RESOLVED items
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of nostd audit document completeness and accuracy</name>
  <files>docs/nostd-audit.md</files>
  <action>
    Review docs/nostd-audit.md against the checklist below. If any item is incorrect, fix the document before marking approved.
  </action>
  <verify>
    <automated>grep "Gap Priority Ranking" /home/ben/github.com/bharrisau/esp32-gnssmqtt/docs/nostd-audit.md && grep "RESOLVED" /home/ben/github.com/bharrisau/esp32-gnssmqtt/docs/nostd-audit.md && echo "checkpoint pre-check OK"</automated>
  </verify>
  <done>Human has confirmed the audit document is accurate and complete per the verification checklist.</done>
  <what-built>docs/nostd-audit.md — complete ESP-IDF dependency audit covering all 12 categories with status mappings and gap priority ranking</what-built>
  <how-to-verify>
    1. Open docs/nostd-audit.md
    2. Verify all 12 categories are present with their tables
    3. Confirm SoftAP password is marked RESOLVED (not GAP)
    4. Confirm DNS hijack is marked SOLVABLE
    5. Confirm log hook (vprintf) is marked SOLVABLE
    6. Confirm Gap Priority Ranking table has 8 rows with Phase assignments matching 23, 24, 25, or Future
    7. Confirm NVS is Rank 1, OTA is Rank 2, SoftAP+DNS is Rank 3
    8. Verify the uxTaskGetStackHighWaterMark entry is marked REPLACED (no embassy equivalent; remove from diagnostics)
    9. Check that TLS blocker detail mentions TLS 1.2 limitation in embedded-tls and the CA bundle problem
  </how-to-verify>
  <resume-signal>Type "approved" if the audit document is accurate and complete, or describe any missing categories or incorrect status assignments</resume-signal>
</task>

</tasks>

<verification>
1. `ls docs/nostd-audit.md` — file exists
2. `grep -c "| " docs/nostd-audit.md` — returns a large number (many table rows)
3. `grep "Gap Priority Ranking" docs/nostd-audit.md` — section header found
4. `grep "RESOLVED" docs/nostd-audit.md` — at least 2 matches (SoftAP, DNS hijack)
5. Human review confirms all 12 categories covered and gap ranking is correct
</verification>

<success_criteria>
docs/nostd-audit.md is committed, covers all 12 ESP-IDF usage categories from the firmware, correctly identifies RESOLVED items (SoftAP password, DNS hijack, log hook), documents hard gaps (TLS, HTTP server, OTA on C6), and provides the ranked gap priority table mapping capabilities to Phase 23-25.
</success_criteria>

<output>
After completion, create `.planning/phases/22-workspace-nostd-audit/22-02-SUMMARY.md` with:
- Confirmation that docs/nostd-audit.md is committed
- Summary of gap count by status (SOLVABLE / RESOLVED / GAP / REPLACED / UNKNOWN)
- Any corrections made during human review
</output>
